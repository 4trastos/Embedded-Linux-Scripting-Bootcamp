📝 Ejercicio 01 - Crear grupo y usuario

Crea un script que:
- Cree un grupo llamado `embedded`
- Cree un usuario llamado `linuxuser` asignado a ese grupo
- Use `groupadd` y `useradd` (si existen)
- Sea idempotente (no falle si ya existen)
